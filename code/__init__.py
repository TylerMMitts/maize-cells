# Package marker for the project source.
#
# Makes code/ importable as a package so every module can be reached by its
# full path (code.measurements.x rather than a bare x), which is what lets a
# script run from any working directory.
