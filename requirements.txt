# Every third-party package imported anywhere under code/, checked against the
# source rather than written by hand. Versions are the ones this project has
# been run with; they are lower bounds, not pins.

# Core numerics and data handling
numpy>=2.2
scipy>=1.15
pandas>=2.3

# Plotting
matplotlib>=3.10
seaborn>=0.13

# Statistics and modelling
scikit-learn>=1.7
statsmodels>=0.14

# Imaging and segmentation
opencv-python>=4.11
Pillow>=12.0
ultralytics>=8.4

# Deep learning. Install the build that matches your CUDA version from
# pytorch.org; a CPU-only build is enough to run inference.
torch>=2.5

# Geometry, used to build the printable and FEA solids. Needs GEOS 3.13 or
# newer for constrained_delaunay_triangles, which the STL export relies on.
shapely>=2.1

# Graphs, used by the neighbour analysis
networkx>=3.4

# Topological data analysis. Only needed for code/tda/; the rest of the
# pipeline runs without these.
gudhi>=3.11
ripser>=0.6
persim>=0.3
kepler-mapper>=2.0

# Optional mask refinement. Off by default (USE_SAM_REFINEMENT in config.py),
# so this is only needed if that is turned on.
mobile-sam>=1.0

# Utilities
tqdm>=4.67
psutil>=7.0
PyYAML>=6.0
